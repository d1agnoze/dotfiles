# if running bash
if [ -n "$BASH_VERSION" ]; then
    # include .bashrc if it exists
    if [ -f "$HOME/.bashrc" ]; then
	. "$HOME/.bashrc"
    fi
fi
# run exports
. "$HOME/dotfiles/.bash_env"
. "$HOME/.cargo/env"
